[Custom]

[Arch]
base-devel>if (which yay) then echo 'yay does exist'; else $(git clone https://aur.archlinux.org/yay.git && cd yay && makepkg -si ; cd .. ; rm -rf yay); fi
kitty
neovim
bluez>systemctl enable bluetooth;systemctl start bluetooth
bluez-utils
